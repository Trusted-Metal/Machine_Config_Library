#pragma once
// Machine Config Library — adapter base type.
// Adapters transform a MachineConfig in-place to handle schema version migrations.
// No adapters are defined until the first schema bump requires one.

#include "machine_config/models.hpp"

namespace machine_config {

// Adapter base defined here when the first adapter is written.

} // namespace machine_config
