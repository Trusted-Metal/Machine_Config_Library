#pragma once
// Machine Config Library — adapter registry.
// Resolves and applies the adapter chain for a given schema version.
// Empty until the first schema bump.

#include "machine_config/adapters/adapter.hpp"

namespace machine_config {

// AdapterRegistry defined here when the first adapter is written.

} // namespace machine_config
