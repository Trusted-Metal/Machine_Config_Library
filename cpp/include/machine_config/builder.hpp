#pragma once
// Machine Config Library — config builder helper.
// Mirrors python/src/machine_config/builder.py (MockConfigBuilder pattern).
// Implementation deferred until after reader and writer are complete.

#include "machine_config/models.hpp"

namespace machine_config {

// Builder helpers defined here after reader and writer are stable.

} // namespace machine_config
